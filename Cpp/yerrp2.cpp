#include <iostream> // This is C++ Vocabulary

using namespace std;

int main ()
{
    
    char grade = 'A'; //char is used to place a single character

    string phrase = "Suny Cobleskill"; //strings are plaintext, numerous characters

    int age = 27; //int = interger. used to store whole numer values

    float cumulativeGpa = 3.5; //used to place decimal value, just use double though lol

    double semesterGpa = 3.85;  // more decimal places than a float

    bool isMale = true;  //bool allows us to use T & F values

    cout << grade << endl; // This code is a constant, using this will output the value inbetween the (<< <<). it can be a value from the varibles or anything else, if it is between the arrows it will be outputted


  

    return 0;
}